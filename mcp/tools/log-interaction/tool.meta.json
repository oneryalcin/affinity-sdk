{
  "name": "log-interaction",
  "description": "Log an interaction (call, meeting, email, chat message, or in-person meeting) with one or more people.",
  "inputSchema": {
    "type": "object",
    "required": ["type", "personIds"],
    "properties": {
      "type": {
        "type": "string",
        "enum": ["call", "meeting", "email", "chat_message", "in_person"],
        "description": "Type of interaction"
      },
      "personIds": {
        "type": "array",
        "items": {"type": "integer"},
        "minItems": 1,
        "description": "Person IDs who participated in the interaction"
      },
      "subject": {
        "type": "string",
        "description": "Subject line or title of the interaction"
      },
      "body": {
        "type": "string",
        "description": "Content or notes about the interaction"
      },
      "date": {
        "type": "string",
        "format": "date-time",
        "description": "When the interaction occurred (ISO 8601 format, defaults to now)"
      },
      "direction": {
        "type": "string",
        "enum": ["incoming", "outgoing"],
        "description": "Direction of the interaction (for calls/emails)"
      }
    }
  },
  "annotations": {
    "readOnlyHint": false
  },
  "timeoutSecs": 15
}
