{
  "name": "set-workflow-status",
  "description": "Update the status of a workflow item on a list. Status can be specified as text (e.g., 'Active', 'Closed Won') or as an option ID.",
  "inputSchema": {
    "type": "object",
    "required": ["listId", "listEntryId", "status"],
    "properties": {
      "listId": {
        "type": "integer",
        "description": "List ID"
      },
      "listEntryId": {
        "type": "integer",
        "description": "List entry ID (use resolve-workflow-item to find this)"
      },
      "status": {
        "type": "string",
        "description": "Status value (text name or option ID)"
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true
  },
  "timeoutSecs": 15
}
