{
  "name": "update-workflow-fields",
  "description": "Update multiple fields on a workflow item at once. Fields can be specified by name or ID.",
  "inputSchema": {
    "type": "object",
    "required": ["listId", "listEntryId", "fields"],
    "properties": {
      "listId": {
        "type": "integer",
        "description": "List ID"
      },
      "listEntryId": {
        "type": "integer",
        "description": "List entry ID (use resolve-workflow-item to find this)"
      },
      "fields": {
        "type": "object",
        "description": "Object mapping field names or IDs to values",
        "additionalProperties": true
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true
  },
  "timeoutSecs": 30
}
